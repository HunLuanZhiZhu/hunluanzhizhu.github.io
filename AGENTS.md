# AGENTS.md

本文件是仓库级开发指令，适用于整个 `hunluanzhizhu.github.io` 仓库。进入具体子目录工作前，先确认目标子项目；如果子目录存在更近的 `AGENTS.md` / `CLAUDE.md`，以更近的指令为准。

## Repository scope

这是一个部署到 GitHub Pages 的个人静态网页项目合集，不是单体应用。

- 根 `index.html` 负责项目导航。
- `projects/<slug>/` 下的子项目彼此独立，技术栈和历史约束可能不同。
- 不要为了统一架构而跨项目重构、抽共享框架或引入统一构建链，除非用户明确要求。
- 涉及具体业务逻辑时，以用户当前要求和当前源码为准；README 可能滞后，不应被当作唯一事实来源。

## Before editing

1. 先运行 `git status --short`，确认并保留用户已有未提交修改。
2. 明确本次目标子项目，避免顺手修改无关目录。
3. 阅读目标项目的 README（如存在）、入口文件和与任务直接相关的源码。
4. 若目标目录有更具体的 agent 指令，先读取后再修改。
5. 不要因为缺少统一测试框架就擅自引入 Node、Vite、Webpack、React 等依赖或构建系统。

## Implementation principles

- 保持现有静态托管和直接访问能力。
- 优先最小、局部修改，遵循目标子项目现有风格。
- 不跨项目搬运业务逻辑、样式体系或状态管理方式，除非任务本身要求这样做。
- 修改 HTML/CSS/JS 时兼顾现有桌面端、移动端、深色模式和可访问性行为。
- 不随意修改、重编码或替换大型二进制/媒体资源。
- 不提交 `.gitignore` 已排除的临时文件、测试素材或生成物。
- 新增子项目时，在 `projects/<slug>/` 下创建，并同步更新根 `index.html` 的项目注册信息。

## Validation

仓库没有统一构建或测试流程，验证方式应跟随目标子项目。

默认从仓库根目录通过 HTTP 预览；如果子项目 README 指定了特殊启动方式或服务器要求，必须遵守该项目说明。

完成修改后至少检查：

- 本次涉及的页面/交互可以实际走通。
- 浏览器无与本次修改相关的明显 console error。
- `git diff --check` 无空白错误。
- `git diff` / `git status` 只包含本任务应有的改动。

如果没有可执行的自动化测试，只说明实际做过的验证，不要声称“全部测试通过”。

## Documentation

当修改数据结构、导入导出格式、用户工作流、运行方式或关键资源路径时，同步更新目标子项目已有的 README 或内置说明。

不要为了与旧文档一致而回退已经明确更新过的实现；应修正文档，使其与当前行为一致。

## Git and delivery

- 不覆盖用户已有未提交修改。
- 不执行 `reset --hard`、强制 checkout、强推等破坏性 Git 操作，除非用户明确要求。
- 不自动提交，除非用户明确要求 commit。
- 用户要求提交时，先检查 diff 并完成适用验证，再提交。
- 交付时说明改了哪些文件、验证了什么，以及仍未验证的部分。
